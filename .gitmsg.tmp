fix: 修复 review 发现的 11 个设计缺陷 + 补充测试用例（28 例全绿）

P0（真 bug）：
- Team 引导段注入 Worker：缺 agent.id === sid 守卫，Worker 也收到
  你是主窗口请用 task_create 派发 导致误认主窗口。这是上一轮身份错乱的真正根因
- vt 函数重复定义（index.mjs 本地覆盖了 core.mjs import）

P1（健壮性）：
- settleRun 加 try-catch 防未处理 Promise rejection
- board_verdict 驳回路径去掉重复 history 记录
- pickProvider 缓存结果（不再每次 spawn 遍历 provider）
- resolveRoot 硬编码 20 层上限改为 visited 回环检测

P2（代码卫生）：
- task_list 排序用 PRIO_RANK 替代本地 po 字面量
- set-board-config 去掉无意义的 minWorkers/minVerifiers
- knownSessions 加 30min TTL 淘汰，防长期运行空转
- 客户端无活跃任务时轮询降频 3s 到 15s
